'use strict';
(async()=>{const s=await window.rhythm.getState(),p=s.runtime.rewardPrompt;if(!p)return window.rhythm.closeWindow();document.querySelector('#title').textContent=p.title;document.querySelector('#message').textContent=p.message;document.querySelector('#balance').textContent=p.balanceMinutes;const input=document.querySelector('#minutes');input.max=p.maxMinutes;input.value=p.maxMinutes;document.querySelector('#use').onclick=()=>window.rhythm.respondReward({use:true,minutes:Number(input.value)});document.querySelector('#decline').onclick=()=>window.rhythm.respondReward({use:false,minutes:0});})();
